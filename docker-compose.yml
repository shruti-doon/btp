version: '3.8'

services:
  frontend:
    build: .
    container_name: cropdarpan-frontend
    ports:
      - "80:80"
    networks:
      - cropdarpan-network

networks:
  cropdarpan-network:
    driver: bridge 